! This file contains commands to be read in for a Pythia8 run. 
! Lines not beginning with a letter, digit or < are comments.
! Names are case-insensitive  -  but spellings-sensitive!

! 1) Settings that could be used in a main program, if desired.
Main:numberOfEvents = 1000         ! number of events to generate
Main:timesAllowErrors = 10         ! abort run after this many flawed events
Next:numberCount = 100             ! print message every n events
Next:numberShowEvent = 0           ! number of events to print
Init:showChangedSettings = on      ! print changed flags/modes/parameters
